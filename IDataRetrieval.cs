using System.Collections.Generic;
using System.Linq;
using contentapi.Models;
using Microsoft.EntityFrameworkCore;

namespace contentapi
{
    //public interface IDataRetrieval
    //{
    //    IQueryable<T> GetAll<T>(IEnumerable<long> ids) where T : GenericModel;
    //    IQueryable<T> GetRaw<T>() where T : GenericModel;
    //    //IQueryable<T> GetList<T>(IEnumerable<long> ids) where T : GenericModel;
    //}
}