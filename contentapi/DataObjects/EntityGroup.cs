using Randomous.EntitySystem;

namespace contentapi
{
    public class EntityGroup
    { 
        public Entity entity; // = new Entity();
        public EntityRelation relation; // = new EntityRelation();
        public EntityValue value; // = new EntityValue();
        public EntityRelation permission; // = new EntityRelation(); 
    }
}